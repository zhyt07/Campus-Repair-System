# 开发环境配置
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/campus_repair?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false
    username: root
    password: 123456

  redis:
    host: localhost
    port: 6379
    password:

logging:
  level:
    com.campus.repair: debug
